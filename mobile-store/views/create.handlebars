<!-- File: views/create.handlebars -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thêm điện thoại mới</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
</head>
<body>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Thêm điện thoại mới</h1>
            <a href="/" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Quay lại
            </a>
        </div>

        <div class="card">
            <div class="card-body">
                <form action="/create" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="name" class="form-label">Tên điện thoại</label>
                        <input type="text" class="form-control {{#if (lookup errors 'name')}}is-invalid{{/if}}" 
                               id="name" name="name" value="{{data.name}}" required>
                        {{#if (lookup errors 'name')}}
                        <div class="invalid-feedback">{{lookup errors 'name'}}</div>
                        {{/if}}
                    </div>

                    <div class="mb-3">
                        <label for="price" class="form-label">Giá</label>
                        <input type="number" class="form-control {{#if (lookup errors 'price')}}is-invalid{{/if}}" 
                               id="price" name="price" value="{{data.price}}" required>
                        {{#if (lookup errors 'price')}}
                        <div class="invalid-feedback">{{lookup errors 'price'}}</div>
                        {{/if}}
                    </div>

                    <div class="mb-3">
                        <label for="brand" class="form-label">Thương hiệu</label>
                        <input type="text" class="form-control {{#if (lookup errors 'brand')}}is-invalid{{/if}}" 
                               id="brand" name="brand" value="{{data.brand}}" required>
                        {{#if (lookup errors 'brand')}}
                        <div class="invalid-feedback">{{lookup errors 'brand'}}</div>
                        {{/if}}
                    </div>

                    <div class="mb-3">
                        <label for="releaseYear" class="form-label">Năm ra mắt</label>
                        <input type="number" class="form-control {{#if (lookup errors 'releaseYear')}}is-invalid{{/if}}" 
                               id="releaseYear" name="releaseYear" value="{{data.releaseYear}}" required>
                        {{#if (lookup errors 'releaseYear')}}
                        <div class="invalid-feedback">{{lookup errors 'releaseYear'}}</div>
                        {{/if}}
                    </div>

                    <div class="mb-3">
                        <label for="image" class="form-label">Ảnh sản phẩm</label>
                        <input type="file" class="form-control {{#if (lookup errors 'image')}}is-invalid{{/if}}" 
                               id="image" name="image" accept="image/*" required>
                        {{#if (lookup errors 'image')}}
                        <div class="invalid-feedback">{{lookup errors 'image'}}</div>
                        {{/if}}
                    </div>

                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save"></i> Lưu
                    </button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 
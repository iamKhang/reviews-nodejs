<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paper Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/bootstrap-icons/font/bootstrap-icons.css">
</head>

<body>
    <div class="container mt-4">
        <h1 class="mb-4">Paper Manager</h1>

        <div class="row mb-3">
            <a class="btn btn-success 2 d-block w-25" href="/add">
                Them bai bao moi
            </a>
        </div>

        {{#if error}}
        <div class="alert alert-danger">{{error}}</div>
        {{/if}}


        <table class="table table-responsive table-striped table-hover">
            <thead class="table-dark">
            <tr>
                <th>ISBN</th>
                <th>Tieu de</th>
                <th>So trang</th>
                <th>Anh bia</th>
                <th>Nhom tac gia</th>
                <th >
                    <i class="bi bi-book"></i>
                </th>
            </tr>
        </thead>
        <tbody>
            {{#if items}}
            {{#each items}}
            <tr>
                <td>{{isbn}}</td>
                <td>{{paperTitle}}</td>
                <td>{{pages}}</td>
                <td>
                    <img src="{{coverImageUrl}}" alt="{{title}}" style="width: 120px; height: 170px; object-fit: cover;">
                </td>
                <td>{{authors}}</td>
            </tr>
            {{/each}}
            {{else}}
            <p class="text-muted">No papers found.</p>
            {{/if}}
        </tbody>
        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>